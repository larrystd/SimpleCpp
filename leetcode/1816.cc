#include <cstring>
#include <string>
using namespace std;

class Solution {
public:
    string truncateSentence(string s, int k) {
        char* str = s.data();
        char* p;
        p = strtok(str, " ");
        int num = 0;
        while (!p) {
            s.substr()
        }
    }
};